{
  "name": "doggodates",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "agent"
  ],
  "scripts": {
    "build": "npm run build --workspace doggodates-agent",
    "typecheck": "npm run typecheck --workspace doggodates-agent",
    "test": "npm run test --workspace doggodates-agent",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2"
  }
}
